# RAG Books

Place plaintext or markdown book files in this folder. The server will perform a simple keyword match to include relevant snippets into the ChatBot context.

Example:

- circuits_basics.md
- teacher_notes_week1.txt

Do not place sensitive information here; the contents may be sent to the LLM as context.
